<active-form
        v-model="{{ $form['model'] }}"
        :content="{{ $form['content'] }}"
        :object-default="{{ $form['default'] }}"
        :template="{{ $form['template'] }}"
></active-form>
